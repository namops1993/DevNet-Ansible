---
- name: Connecting device
  hosts: devices
  connection: network_cli
  tasks:
    - name: Turn on Interface
      ios_interfaces:
        config:
          - name: "{{ item.name }}"
            enabled: True
        state: merged
      loop: "{{ interfaces }}"

    - name: Configure interface
      ios_l3_interface:
        name: "{{ item.name }}"
        ipv4: "{{ item.ipv4 }}"
        stage: present
      loop: "{{ interfaces }}"
